{
  "name": "node-express-test-example",
  "version": "1.0.0",
  "description": "An example of testing node and express routes with mocha and chai with nyc for coverage",
  "main": "server.js",
  "author": "Terra Taylor",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.1",
    "config": "^1.31.0",
    "express": "^4.17.2",
    "morgan": "^1.7.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-http": "^2.0.1",
    "mocha": "^2.5.3",
    "nyc": "^15.1.0"
  },
  "scripts": {
    "start": "SET NODE_ENV=dev && node server.js",
    "test": "nyc --reporter=text mocha --timeout 10000"
  }
}
